import React from 'react'


export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;
const math = () => {
  return (
    <div>
      plugin
    </div> 
      
  )
}

export default math